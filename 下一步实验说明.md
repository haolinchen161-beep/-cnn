# 下一步实验说明：R=3 每阶独立 A-head

## 目标

当前 10 阶模型已经说明：

```text
固有频率 omega 可以学到约 1% 量级；
模态留数 A 的幅值误差仍然较大。
```

因此下一步不继续硬训 10 阶，而是先验证低阶残量是否可学：

```text
结构图 + 激励点 + 底面查询点
→ omega_1:3
→ A_q,1:3
```

---

## 为什么先做 3 阶

1. 前 10 阶里高阶/中阶 A 难度不一致，混在一起会互相干扰。
2. 频率已经能学，当前瓶颈是 A。
3. 如果前 3 阶 A 都学不好，继续做 10 阶没有意义。
4. 如果前 3 阶明显改善，再扩展到前 5 阶、前 10 阶。

---

## 程序变化

`run_meshgraph_modal.py` 当前会：

1. 读取原始 10 阶 HDF5；
2. 在入口层面截取：

```text
modal_omega[:3]
modal_residue_z[:, :3]
```

3. 使用 `PerModeResidueNet`：

```text
shared MeshGraph encoder
        ↓
omega head → ω1, ω2, ω3
A head 1 → A_q1
A head 2 → A_q2
A head 3 → A_q3
```

4. 只对凹槽底面区域训练：

```text
TARGET_REGION = "bottom"
```

---

## 运行命令

```powershell
F:/pytorch_cuda12/python.exe -B run_meshgraph_modal.py
```

输出目录：

```text
runs/下一步_R3_每阶A头_bottom/
```

---

## 重点观察指标

训练完后重点看：

```text
val_w10_mean_pct       # 频率误差，R=3时实际是前三阶
val_A_vis_mean_pct     # 可见 A 的整体误差
val_A_top_mean_pct     # 强 A 区域误差
val_A_sign_mean_pct    # A 符号准确率
val_Y_mean_pct         # signed-asinh 空间误差
```

判断标准：

```text
1. val_w10_mean_pct 仍接近 1%~2%；
2. val_A_sign_mean_pct 明显高于原 10 阶模型；
3. val_A_vis_mean_pct / val_A_top_mean_pct 比原 10 阶明显下降。
```

如果这些指标不改善，优先检查：

```text
1. 6 mm 网格下底面 A 标签是否噪声太大；
2. 不同自由网格底面点是否需要先统一参考点；
3. 是否需要 clean dataset：固定 layout、关闭 grid_jitter、离散槽深。
```

---

## 暂时不做的事

当前阶段先不做：

```text
1. 不重新生成大数据集；
2. 不做同拓扑网格；
3. 不加 modal matching；
4. 不预测阻尼；
5. 不扩展到 10 阶。
```

这些等 R=3 结果明确后再决定。
